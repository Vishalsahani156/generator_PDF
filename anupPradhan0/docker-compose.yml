services:
  mongodb:
    image: mongo:7
    container_name: appdb-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: appdb
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
